import type { Metadata } from 'next';
import './globals.css';
import { Header } from '@/components/Header';
import { Footer } from '@/components/Footer';

export const metadata: Metadata = {
  title: 'PetScore - El Pasaporte de Confianza para tu Mascota',
  description: 'Un estándar de confianza y transparencia para la comunidad pet-friendly. Valora el comportamiento de las mascotas y encuentra alojamientos que las aceptan.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="es">
      <body>
        <div className="flex flex-col min-h-screen">
          <Header />
          <main className="flex-grow">{children}</main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
